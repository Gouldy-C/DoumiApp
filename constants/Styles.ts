import { StyleSheet } from 'react-native';

export const constStyles = StyleSheet.create({
  labels: {
    flexDirection:'row', 
    justifyContent: 'center',
    paddingTop: 3,
    gap: 3, 
    marginTop: 5, 
    borderColor: 'black', 
    borderWidth: 1,
    borderRadius: 50,
    width: 70
  },
  postText: {
    fontSize: 18
  }
})